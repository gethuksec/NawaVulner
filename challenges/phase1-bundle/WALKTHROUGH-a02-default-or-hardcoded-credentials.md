# Walkthrough uji: `a02-default-or-hardcoded-credentials`

## Ringkas

1. Buka lab dari dashboard (`/lab/a02-default-or-hardcoded-credentials/`).
2. **View source** halaman login; telusuri komentar commissioning di antara noise vendor/HTML.
3. Kirim `POST /login` (form) dengan pasangan `admin` / `LabBootstrap!77`.
4. Salin flag dari halaman sukses ke platform.

## Hint / write-up

Platform: Hint tier + write-up HTML setelah unlock.
